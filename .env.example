DATABASE_URL="postgresql://postgres:postgres@localhost:5432/google_keep?schema=public"
PORT=4000
JWT_SECRET="replace_me_with_a_long_random_string"
REMINDER_POLL_INTERVAL_MS=15000
REMINDER_RETENTION_DAYS=30
SMTP_HOST="smtp.mailtrap.io"
SMTP_PORT=587
SMTP_USER="mail_user"
SMTP_PASS="mail_password"
SMTP_FROM="Google Keep Clone <no-reply@example.com>"
# Production (Neon) example:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
